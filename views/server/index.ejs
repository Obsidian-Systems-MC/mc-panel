<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Obsidian Sys | <%= server.name %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/style.css">
</head>
<body class="bg-gray-900">
    <div class="w-screen mt-[20%] px-[5%]">
        <div class="grid grid-cols-2">
            <div class="">
                <div>
                    <h1 class="text-3xl font-bold">Huh, it looks like this server is managed by Obsidian.</h1>
                    <p>Doesn't mean you can't manage the fun yourself though!</p>
        
                    <div class="mt-[50px]" id="controls">
                        <div class="grid grid-cols-5 gap-4">
                            <div class="button bg-green-800 disabled text-center p-[10px] rounded-md shadow-md">
                                Start 
                            </div>
                            <div class="button bg-red-800 disabled text-center p-[10px] rounded-md shadow-md">
                                Stop
                            </div>
                            <div class="button bg-red-900 disabled text-center p-[10px] rounded-md shadow-md">
                                ⚠ Kill
                            </div>
                            <div class="button bg-orange-500 disabled text-center p-[10px] rounded-md shadow-md">
                                Ping Support
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div>
                <div class="bg-gray-800 p-3 rounded-md shadow-mg">
                    <div class="text-gray-200">Logs from <span class="font-bold"><%= server.name %></span>, hosted on <span class="font-bold"><%= node.fqdn %> (<%= node.name %>)</span></div>
                    <div class="mt-[80px]">
                        <p><span class="text-gray-400">Please wait, logs are loading...</span> <span class="text-red-400 font-bold">this may take a while.</span></p>
                    </div>
                </div>
                <div class="bg-gray-800 p-3 rounded-md shadow-mg mt-[20px]">
                    <div class="text-gray-200"><span class="font-bold">Server Information</span></div>
                    <div class="mt-[10px]">
                        <p><span class="font-bold">Server Memory:</span> <%= server.limits.memory %> MB</p>
                        <p><span class="font-bold">vCPU Limit:</span> <%= server.limits.cpu == 0 ? `Unmetered` : `${server.limits.cpu} vCPU Cores` %></p>
                        <p><span class="font-bold">Disk Space:</span> <%= Number(server.limits.disk).toFixed(2) / 1024 %> GB</p>
                        <p><span class="font-bold">Support Identifer:</span> <code class="bg-gray-900 p-2 rounded-md"><%= server.identifier %></code></p>
                    </div>
                </div>
            </div>
        </div>
    </div>

</body>
</html>